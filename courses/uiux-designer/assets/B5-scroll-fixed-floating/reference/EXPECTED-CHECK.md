# B5 完成檢查表：滾動、置頂導覽與漂浮按鈕

## 設定

- [ ] `Screen / Task list` 是 402×874，內容至少十筆。
- [ ] Overflow Before 已記錄，After 為 `Vertical`。
- [ ] Flow starting point 從 Task list 開始。

## Preview

| 檢查 | Expected | Actual | 結果 |
|---|---|---|---|
| 上滑一次 | 看見清單後段 |  | PASS／FAIL／NOT_RUN |
| 可見任務編號 | 第 10 筆或更後段 |  | PASS／FAIL／NOT_RUN |
| 水平位移 | 沒有水平位移 |  | PASS／FAIL／NOT_RUN |

瀏覽器／日期：

Preview URL／檔案：

## 固定與漂浮層

- Header 圖層名稱：
- 固定設定截圖：
- 上滑後 Header 是否可見：是／否／NOT_RUN
- Action 圖層名稱：
- 按鈕位置與安全區：
- 是否遮住任務文字：否／是／NOT_RUN
- 聯合行為狀態：PASS／FAIL／MACHINE_PASS_PARTIAL

## Solo

只把十筆任務改成十二筆，保持 Frame、Overflow、Header 與 Action 位置不變。

變化紀錄：

## 交接給 B6

請寫下 case_id、Overflow 值、可見任務編號、Header 狀態、按鈕遮擋結果與仍待人工驗證的項目。
